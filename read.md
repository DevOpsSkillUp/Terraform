Terraform Commands:

    terraform init 
    terraform plan
    terraform validate
    terraform apply [or] terraform apply -auto-approve 
    terraform destroy [or] terraform destroy -auto-approve

AWS Setup:
    
    create iam user 
    install aws cli v2 
    aws configure 